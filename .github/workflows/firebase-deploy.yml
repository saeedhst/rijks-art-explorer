# This file was auto-generated by the Firebase CLI
# https://github.com/firebase/firebase-tools

name: Sanbox
on:
  push:
    branches:
      - sandbox
jobs:
  build_and_deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Inspect variables
        run: |
          echo "repository variable : $API_URL"
        env:
          API_URL: ${{ secrets.GITHUB_TOKEN }}
          
